version: '3.8'

services:
  app:
    build: .
    ports:
      - "4444:4444"
    volumes:
      - uploads:/app/uploads
      - videos:/app/videos
      - dbdata:/app/data
    restart: unless-stopped

volumes:
  uploads:
  videos:
  dbdata: